# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is a **GitHub profile repository** (`bariiitdu/bariiitdu`). The `README.md` at the root is automatically rendered on the GitHub profile page at github.com/bariiitdu. There is no build system, test suite, or application code.

## Working with This Repository

The sole artifact is `README.md` — a Markdown file displayed publicly as the GitHub profile. Edits to it are immediately reflected on the profile once pushed to `main`.

There are no build, lint, or test commands to run. Changes are made directly to `README.md` and committed.
